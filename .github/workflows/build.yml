name: Build AMXX Plugin

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build_linux:
    name: Build (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install AMX Mod X compiler 1.10 (Linux)
        run: |
          wget -q https://github.com/alliedmodders/amxmodx/releases/download/1.10.0.5481/amxmodx-1.10.0-git5481-base-linux.tar.gz -O amxmodx.tar.gz
          tar -xzf amxmodx.tar.gz
          ls -lh addons/amxmodx/scripting/amxxpc*
          AMXXPC=$(find . -name "amxxpc" -type f | head -n1)
          AMXXPC_DIR=$(dirname "$AMXXPC")
          echo "AMXXPC=$AMXXPC" >> $GITHUB_ENV
          echo "AMXXPC_DIR=$AMXXPC_DIR" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=$AMXXPC_DIR:$LD_LIBRARY_PATH" >> $GITHUB_ENV
          ls -la "$AMXXPC" "$AMXXPC_DIR/amxxpc32.so"
          chmod +x "$AMXXPC"

      - name: Compile valorantkillsound.sma
        run: |
          mkdir -p amxmodx/scripting/compiled
          echo "Using compiler: $AMXXPC (dir $AMXXPC_DIR)"
          ls -la "$AMXXPC_DIR/amxxpc"*
          ls -la amxmodx/scripting/
          LD_LIBRARY_PATH="$AMXXPC_DIR:$LD_LIBRARY_PATH" "$AMXXPC" amxmodx/scripting/valorantkillsound.sma -oamxmodx/scripting/compiled/valorantkillsound.amxx -iamxmodx/scripting/include -iaddons/amxmodx/scripting/include
          ls -lh amxmodx/scripting/compiled/
          test -f amxmodx/scripting/compiled/valorantkillsound.amxx || { echo "Compile failed - artifact missing" >&2; exit 1; }

      - name: Upload artifact (Linux)
        uses: actions/upload-artifact@v4
        with:
          name: valorantkillsound-linux.amxx
          path: amxmodx/scripting/compiled/valorantkillsound.amxx
          if-no-files-found: error

  build_windows:
    name: Build (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install AMX Mod X compiler 1.10 (Windows)
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri https://github.com/alliedmodders/amxmodx/releases/download/1.10.0.5481/amxmodx-1.10.0-git5481-base-windows.zip -OutFile amxmodx.zip
          Expand-Archive amxmodx.zip -DestinationPath amxmodx_win -Force
          Get-ChildItem -Recurse -Filter "amxxpc*" -Path amxmodx_win | Format-Table FullName,Length
          $AMXXPC = Get-ChildItem -Recurse -Filter "amxxpc.exe" -Path amxmodx_win | Select-Object -First 1 -ExpandProperty FullName
          $AMXXPC_DIR = Split-Path $AMXXPC
          "AMXXPC=$AMXXPC" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "AMXXPC_DIR=$AMXXPC_DIR" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Get-ChildItem $AMXXPC_DIR/amxxpc* | Format-List Name,Length,FullName

      - name: Compile valorantkillsound.sma
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path amxmodx/scripting/compiled -Force | Out-Null
          Write-Host "Using compiler: $env:AMXXPC (dir $env:AMXXPC_DIR)"
          Get-ChildItem "$env:AMXXPC_DIR/amxxpc*" | Format-Table Name,Length
          Get-ChildItem amxmodx/scripting/ | Format-Table Name,Length
          & $env:AMXXPC "amxmodx\scripting\valorantkillsound.sma" "-oamxmodx\scripting\compiled\valorantkillsound.amxx" "-iamxmodx\scripting\include" "-iamxmodx_win\addons\amxmodx\scripting\include"
          Get-ChildItem amxmodx/scripting/compiled/ | Format-Table Name,Length
          if (-not (Test-Path amxmodx/scripting/compiled/valorantkillsound.amxx)) { throw "Compile failed - artifact missing" }

      - name: Upload artifact (Windows)
        uses: actions/upload-artifact@v4
        with:
          name: valorantkillsound-windows.amxx
          path: amxmodx/scripting/compiled/valorantkillsound.amxx
          if-no-files-found: error
